///
/// LLMTurnFinishReason.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::mlxreactnative {

  /**
   * An enum which can be represented as a JavaScript union (LLMTurnFinishReason).
   */
  enum class LLMTurnFinishReason {
    COMPLETED      SWIFT_NAME(completed) = 0,
    STOPPED      SWIFT_NAME(stopped) = 1,
    SUPERSEDED      SWIFT_NAME(superseded) = 2,
    UNLOADED      SWIFT_NAME(unloaded) = 3,
    FAILED      SWIFT_NAME(failed) = 4,
    TOOL_CALLS      SWIFT_NAME(toolCalls) = 5,
    LENGTH      SWIFT_NAME(length) = 6,
  } CLOSED_ENUM;

} // namespace margelo::nitro::mlxreactnative

namespace margelo::nitro {

  // C++ LLMTurnFinishReason <> JS LLMTurnFinishReason (union)
  template <>
  struct JSIConverter<margelo::nitro::mlxreactnative::LLMTurnFinishReason> final {
    static inline margelo::nitro::mlxreactnative::LLMTurnFinishReason fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("completed"): return margelo::nitro::mlxreactnative::LLMTurnFinishReason::COMPLETED;
        case hashString("stopped"): return margelo::nitro::mlxreactnative::LLMTurnFinishReason::STOPPED;
        case hashString("superseded"): return margelo::nitro::mlxreactnative::LLMTurnFinishReason::SUPERSEDED;
        case hashString("unloaded"): return margelo::nitro::mlxreactnative::LLMTurnFinishReason::UNLOADED;
        case hashString("failed"): return margelo::nitro::mlxreactnative::LLMTurnFinishReason::FAILED;
        case hashString("tool_calls"): return margelo::nitro::mlxreactnative::LLMTurnFinishReason::TOOL_CALLS;
        case hashString("length"): return margelo::nitro::mlxreactnative::LLMTurnFinishReason::LENGTH;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum LLMTurnFinishReason - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::mlxreactnative::LLMTurnFinishReason arg) {
      switch (arg) {
        case margelo::nitro::mlxreactnative::LLMTurnFinishReason::COMPLETED: return JSIConverter<std::string>::toJSI(runtime, "completed");
        case margelo::nitro::mlxreactnative::LLMTurnFinishReason::STOPPED: return JSIConverter<std::string>::toJSI(runtime, "stopped");
        case margelo::nitro::mlxreactnative::LLMTurnFinishReason::SUPERSEDED: return JSIConverter<std::string>::toJSI(runtime, "superseded");
        case margelo::nitro::mlxreactnative::LLMTurnFinishReason::UNLOADED: return JSIConverter<std::string>::toJSI(runtime, "unloaded");
        case margelo::nitro::mlxreactnative::LLMTurnFinishReason::FAILED: return JSIConverter<std::string>::toJSI(runtime, "failed");
        case margelo::nitro::mlxreactnative::LLMTurnFinishReason::TOOL_CALLS: return JSIConverter<std::string>::toJSI(runtime, "tool_calls");
        case margelo::nitro::mlxreactnative::LLMTurnFinishReason::LENGTH: return JSIConverter<std::string>::toJSI(runtime, "length");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert LLMTurnFinishReason to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("completed"):
        case hashString("stopped"):
        case hashString("superseded"):
        case hashString("unloaded"):
        case hashString("failed"):
        case hashString("tool_calls"):
        case hashString("length"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
