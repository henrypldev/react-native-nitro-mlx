///
/// LLMToolExecution.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::mlxreactnative {

  /**
   * An enum which can be represented as a JavaScript union (LLMToolExecution).
   */
  enum class LLMToolExecution {
    PARALLEL      SWIFT_NAME(parallel) = 0,
    SEQUENTIAL      SWIFT_NAME(sequential) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::mlxreactnative

namespace margelo::nitro {

  // C++ LLMToolExecution <> JS LLMToolExecution (union)
  template <>
  struct JSIConverter<margelo::nitro::mlxreactnative::LLMToolExecution> final {
    static inline margelo::nitro::mlxreactnative::LLMToolExecution fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("parallel"): return margelo::nitro::mlxreactnative::LLMToolExecution::PARALLEL;
        case hashString("sequential"): return margelo::nitro::mlxreactnative::LLMToolExecution::SEQUENTIAL;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum LLMToolExecution - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::mlxreactnative::LLMToolExecution arg) {
      switch (arg) {
        case margelo::nitro::mlxreactnative::LLMToolExecution::PARALLEL: return JSIConverter<std::string>::toJSI(runtime, "parallel");
        case margelo::nitro::mlxreactnative::LLMToolExecution::SEQUENTIAL: return JSIConverter<std::string>::toJSI(runtime, "sequential");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert LLMToolExecution to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("parallel"):
        case hashString("sequential"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
